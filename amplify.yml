version: 1
backend:
  phases:
    # IMPORTANT - npm run-script build
    build:
      commands:
        - '# Execute Amplify CLI with the helper script'
        - amplifyPush --simple
frontend:
  phases:
    build:
      commands: []
  artifacts:
    # IMPORTANT - /items
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
